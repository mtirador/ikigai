/*

const respuesta = {
    '0': 'No',
    '1': 'Si'
};

document.querySelectorAll('.pensamientos-index .grid-view .table tbody tr td:nth-child(2)').forEach(
    td => {
        const valor = td.textContent.trim(); //Elimina los espacios 
        td.textContent = respuesta[valor] || '-';
    }
  );  
document.querySelectorAll('.pensamientos-index .grid-view .table tbody tr td:nth-child(3)').forEach(
    td => {
        const valor = td.textContent.trim(); //Elimina los espacios 
        td.textContent = respuesta[valor] || '-';
    }
  );          
     document.querySelectorAll('.pensamientos-index .grid-view .table tbody tr td:nth-child(4)').forEach(
    td => {
        const valor = td.textContent.trim(); //Elimina los espacios 
        td.textContent = respuesta[valor] || '-';
    }
    );
     

document.querySelectorAll('.emociones-index .grid-view .table tbody tr td:nth-child(3)').forEach(
    td => {
        const valor = td.textContent.trim(); //Elimina los espacios 
        td.textContent = respuesta[valor] || '-';
    }
  );     
     */    
    